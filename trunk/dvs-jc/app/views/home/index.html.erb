<h3>Students <%= link_to '[Add]', new_student_path %></h3>
<ul>
  <% Student.where({}).sort_by(&:first_name).sort_by(&:last_name).each do |student| %>
  <li>
    <%= link_to (student.last_name + ', ' + student.first_name), student %>
    (age <%= student.age %>)
  </li>
<% end %>
</ul>
